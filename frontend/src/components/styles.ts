import { Box, Card, Button, Input, Select, Textarea, Badge } from '@chakra-ui/react'
import { motion } from 'framer-motion'



// 卡片组件样式
export const StyledCard = motion(Card)

// 按钮组件样式
export const StyledButton = motion(Button)

// 输入框组件样式
export const StyledInput = motion(Input)

// 选择框组件样式
export const StyledSelect = motion(Select)

// 文本域组件样式
export const StyledTextarea = motion(Textarea)

// 徽章组件样式
export const StyledBadge = motion(Badge)

// 容器组件样式
export const StyledContainer = motion(Box)

// 英雄区组件样式
export const HeroSection = motion(Box)

// 卡片网格容器
export const CardGrid = motion(Box)
